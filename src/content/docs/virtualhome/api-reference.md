The API reference will document the commands and read models exposed by the VirtualHome server.

## Conventions

- Requests and responses use JSON.
- Validation errors fail explicitly with actionable field information.
- Every state-changing request returns a traceable operation identifier.

## Endpoints

Endpoint details will be added here from the verified runtime contract rather than inferred from the interface.
